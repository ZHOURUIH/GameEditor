#include "TDItemBox.h"

const char* TDItemBox::ID = STR(ID);
const char* TDItemBox::Name = STR(Name);
const char* TDItemBox::Object = STR(Object);
const char* TDItemBox::Count = STR(Count);
const char* TDItemBox::AniCount = STR(AniCount);
