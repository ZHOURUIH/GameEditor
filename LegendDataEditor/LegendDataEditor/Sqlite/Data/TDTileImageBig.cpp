#include "TDTileImageBig.h"

const char* TDTileImageBig::ID = STR(ID);
const char* TDTileImageBig::MapFileName = STR(MapFileName);
const char* TDTileImageBig::AtlasName = STR(AtlasName);
const char* TDTileImageBig::ImageName = STR(ImageName);
