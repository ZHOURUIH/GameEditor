#include "TDTileImageMid.h"

const char* TDTileImageMid::ID = STR(ID);
const char* TDTileImageMid::AtlasName = STR(AtlasName);
const char* TDTileImageMid::ImageName = STR(ImageName);
const char* TDTileImageMid::MapFileID = STR(MapFileID);
