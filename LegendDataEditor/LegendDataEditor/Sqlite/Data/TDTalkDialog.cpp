#include "TDTalkDialog.h"

const char* TDTalkDialog::ID = STR(ID);
const char* TDTalkDialog::Description = STR(Description);
const char* TDTalkDialog::NPC = STR(NPC);
const char* TDTalkDialog::NamePosition = STR(NamePosition);
const char* TDTalkDialog::Speaker = STR(Speaker);
const char* TDTalkDialog::Content = STR(Content);
