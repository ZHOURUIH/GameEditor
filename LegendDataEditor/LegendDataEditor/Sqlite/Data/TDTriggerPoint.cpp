#include "TDTriggerPoint.h"

const char* TDTriggerPoint::ID = STR(ID);
const char* TDTriggerPoint::MapID = STR(MapID);
const char* TDTriggerPoint::MapName = STR(MapName);
const char* TDTriggerPoint::TilePosition = STR(TilePosition);
const char* TDTriggerPoint::Event = STR(Event);
