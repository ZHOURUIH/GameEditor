#include "TDItemConsumable.h"

const char* TDItemConsumable::ID = STR(ID);
const char* TDItemConsumable::Name = STR(Name);
const char* TDItemConsumable::DisplayFormat = STR(DisplayFormat);
const char* TDItemConsumable::StdMode = STR(StdMode);
const char* TDItemConsumable::BuffList = STR(BuffList);
const char* TDItemConsumable::MaxUseCount = STR(MaxUseCount);
const char* TDItemConsumable::AniCount = STR(AniCount);
