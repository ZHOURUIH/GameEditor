#include "TDMineRegion.h"

const char* TDMineRegion::ID = STR(ID);
const char* TDMineRegion::MapID = STR(MapID);
const char* TDMineRegion::MineTile = STR(MineTile);
const char* TDMineRegion::Direction = STR(Direction);
